{
  "name": "gateway",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate": "wrangler d1 execute mcp-gateway-db --file=./schema.sql",
    "db:migrate:remote": "wrangler d1 execute mcp-gateway-db --remote --file=./schema.sql"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "typescript": "^5.3.3",
    "wrangler": "^4.58.0"
  },
  "dependencies": {
    "hono": "^4.6.0"
  }
}
